pre-commit:
  commands:
    check-index:
      run: bash Scripts/Quality/check-index-snapshot.sh

pre-push:
  commands:
    check-pushed-tips:
      run: bash Scripts/Quality/check-pushed-tips.sh
      use_stdin: true
